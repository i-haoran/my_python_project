**综合练习题：学生成绩分析系统**

编写程序实现以下功能：

1. 定义函数 `calc_average(scores)`：接收成绩列表，返回平均分（保留2位小数）
2. 定义函数 `get_grade_level(avg)`：根据平均分返回等级：
   - >= 90: 'A' | 80-89: 'B' | 70-79: 'C' | 60-69: 'D' | < 60: 'F'
3. 定义函数 `process_students(students)`：处理学生数据
   - 用 `map` 给每个学生添加 `average` 和 `grade` 字段
   - 用 `filter` 筛选出等级不是'F'的学生
   - 用 `sorted` 按平均分降序排序
   - 返回处理后的列表
4. 主程序：
   - 创建5个学生的列表，每个学生格式：`{"name": "姓名", "scores": [成绩1, 成绩2, 成绩3]}`
   - 调用 `process_students` 处理
   - 遍历输出结果：`姓名 - 成绩 - 平均分 - 等级`

要求：尽量使用高阶函数和lambda表达式，函数需加docstring说明功能。